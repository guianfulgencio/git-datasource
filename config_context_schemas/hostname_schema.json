{
  "_metadata": {
    "description": "Schema for hostname objects.",
    "name": "Config Context schema for hostname"
  },
  "data_schema": {
  "type": "object",
  "properties": {
    "hostname": {
      "type": "string",
      "format": "hostname"
    }
  },
  "required": ["hostname"]
 }
}
