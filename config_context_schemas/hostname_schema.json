{
  "_metadata": {
    "description": "Schema for NTP objects.",
    "name": "Config Context for host"
  },
  "data_schema": {
  "type": "object",
  "properties": {
    "hostname": {
      "type": "string",
      "format": "hostname"
    }
  },
  "required": ["hostname"]
}
